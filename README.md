# Replace values(special characters) in a string

### To run this app
clone it :
```
git@github.com:JavaScript0007/replace-value-in-a-string.git
```

and run a command in your terminal
```
node index.js
```
